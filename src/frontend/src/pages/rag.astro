---
import Layout from '../layouts/Layout.astro'
---

<Layout title='RAG - Retrieval Augmented Generation'>
  <main transition:animate='fade'>
    <div class='rag-container'>
      <h1>RAG (Retrieval Augmented Generation)</h1>
      <div class='rag-content'>
        <p class='description'>
          RAG combines the power of retrieval-based and generative AI models to
          provide accurate, context-aware responses by searching through
          knowledge bases and generating answers based on retrieved information.
        </p>
        <div class='placeholder-section'>
          <p>🚧 RAG functionality coming soon</p>
          <p class='hint'>
            This page will provide tools for document indexing, semantic search,
            and context-aware AI responses.
          </p>
        </div>
      </div>
    </div>
  </main>
</Layout>

<style>
  .rag-container {
    max-width: 1000px;
    margin: 0 auto;
    padding: 2rem 1rem;
  }

  h1 {
    font-size: 2.5rem;
    margin-bottom: 1rem;
    color: var(--text-primary, #100f0f);
    text-align: center;
    transition: color 0.3s ease;
  }

  .rag-content {
    background: var(--bg-primary, white);
    border: 1px solid var(--border-color, #ddd);
    border-radius: 8px;
    padding: 2rem;
    box-shadow: 0 2px 8px var(--shadow, rgba(0, 0, 0, 0.1));
    transition:
      background-color 0.3s ease,
      border-color 0.3s ease;
  }

  .description {
    font-size: 1.1rem;
    line-height: 1.6;
    color: var(--text-primary, #333);
    margin-bottom: 2rem;
    text-align: center;
    transition: color 0.3s ease;
  }

  .placeholder-section {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 300px;
    text-align: center;
    color: var(--text-secondary, #666);
    transition: color 0.3s ease;
  }

  .placeholder-section p {
    margin: 0.5rem 0;
    font-size: 1.1rem;
  }

  .placeholder-section .hint {
    font-size: 0.9rem;
    color: var(--text-tertiary, #999);
    max-width: 600px;
    transition: color 0.3s ease;
  }

  @media screen and (max-width: 768px) {
    h1 {
      font-size: 2rem;
    }

    .rag-content {
      padding: 1.5rem;
    }
  }
</style>
